eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/product/eureka
    register-with-eureka: true    
  instance:
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 30    
